 ThreadLocal:为每一个使用该变量的线程都提供一个变量值的副本，使得每一个线程都可以独立的改变
 该副本的值，而不会和 其它的 线程副本冲突，从 线程的角度来看，就像是每一个线程都完全拥有该变量值

 T get():返回此线程局部变量中，当前局部变量中的值
 void remove()；删除此线程局部变量，当前线程的值
 void set(T value): 设置次线程局部变量，当前线程副本中的值